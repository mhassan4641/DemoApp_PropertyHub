@model List<ProvinceModel>

@{
    ViewData["Title"] = "Province Manager";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="modal fade" id="mydialog" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header bg-primary">
                <h5 id="mydialog-title" class="modal-title"></h5>
                <button type="button" class="close" data-dismiss="modal">
                    <span>&times;</span>
                </button>
            </div>
            <div id="mydialog-body" class="modal-body">
            </div>
        </div>
    </div>
</div>


@section myscript{
    <script>
        $("#createlink,.dgbtn-edit,.dgbtn-delete").click(function (event) {
            event.preventDefault();
            var url = $(this).attr("href");
            var title = $(this).data("dlgtitle");
            $.ajax({
                url: url
            }).done(function (result) {
                $("#mydialog-title").text(title);
                $("#mydialog-body").html(result);
                $("#mydialog").modal();
            });
        });
        
    </script>
}

<div class="container">
    <div>
        <select class="col-3 form-control">
            <option>- Select Option -</option>
            <option>- Select ONE -</option>
            <option>- Select TWO -</option>
        </select>
    </div>
    
</div>


@*<table class="table table-info" id="ptable">
    <thead>
        <tr colspan="3">
            <th>
                Manage Province
                <span>
                    [ <a asp-action="create" asp-controller="Province" class="text-info">
                        Add New
                    </a> ]
                </span>
            </th>
        </tr>
        <tr>
            <th>Province Name</th>
            <th>Province Country</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var m in Model)
        {
            <tr>
                <td>@m.Name</td>
                <td>@m.Country.Name</td>
                <td>
                    <a asp-action="edit" asp-controller="Province" asp-route-id="@m.Id"
                       data-dlgtitle="Edit Province" class="btn btn-warning btn-sm dgbtn-edit">
                        <i class="fa fa-edit"></i>
                    </a>
                    <a asp-action="delete" asp-controller="Province" asp-route-id="@m.Id"
                       data-dlgtitle="Delete Province" class="btn btn-danger btn-sm dgbtn-delete">
                        <i class="fa fa-trash"></i>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>*@